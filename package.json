{
  "name": "reverse-breakout-incremental",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check src/collision.js && node --check src/game.js && node --check server.mjs",
    "test": "node --test"
  }
}
